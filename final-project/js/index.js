 $(document).ready(function()
    {
        // Scroll the whole document
        $('#topMenuLinks').localScroll({
           target:'body'
        });
		$('#intro').localScroll({
           target:'body'
        });
 
    });